[project]
name = "scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "email-validator>=2.3.0",
    "file-shredder",
    "python-gnupg>=0.5.6",
    "utils",
]

[tool.uv.sources]
# file-shredder = { path = "../shredder" }
utils = { path = "../utils" }
