[project]
name = "scripts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "utils",
]

[tool.uv.sources]
utils = { path = "../utils" }
